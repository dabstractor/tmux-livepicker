# Implementation Issue Feedback (Attempt 1/3)

The previous implementation attempt encountered an issue that requires re-planning.

## Issue Details

HALT: fundamental conflict between the PRP's design and an existing race-condition test. The implementation was completed exactly per the PRP verbatim (scripts/input-handler.sh: +source layout.sh; +_lp_scroll_into_view helper using get_state for STATE_CLIENT_WIDTH+STATE_SCROLL; +2 nav call sites; +3 scroll=0 resets). Level 1 passes fully (bash -n clean, shellcheck clean, all 6 grep success-criteria met: source=1, def=1, call-sites=2, resets=3, STATE_SCROLL>=6, tabs-only, 38 additive lines). But Level 2 (tests/run.sh) FAILS reliably: test_superseded_preview_noop (and often test_orig_session_client_aware) fail ~100% of runs with 'stale alpha fire leaked a stray link into the driver'.\n\nROOT CAUSE (proven empirically, not a flake): The PRP's research §7 predicted the suite stays green, but it only reasoned about CORRECTNESS (short lists -> lp_viewport clamps scroll=0 -> no behavior change). It did NOT consider TIMING. The _lp_scroll_into_view helper reads STATE_CLIENT_WIDTH and STATE_SCROLL via get_state, which are each a synchronous tmux show-option round-trip. Every tmux round-trip forces the tmux server to process its event loop, which RUNS pending run-shell -b background jobs. The supersession test fires two rapid next-session navs (seq=1->alpha, seq=2->beta); the stale alpha fire (seq=1) must no-op. It is protected by a mid-function re-check (GUARD 2 in preview.sh) that fires AFTER nav #2 bumps STATE_PREVIEW_SEQ to 2. The window between GUARD 1 (top of preview_main, reads seq) and GUARD 2 (before the unlink) is bridged by nav #2 bumping seq. The helper's 2 extra round-trips on nav #2's PRE-bump path (before _lp_fire_preview bumps seq) both (a) advance alpha's pending fire toward GUARD 2 and (b) delay nav #2's seq bump, so alpha's fire reaches GUARD 2 while seq is still 1 -> it links alpha -> leak.\n\nPROOF OF CAUSATION (clean tree, my change stashed): I added N synthetic 'tmux show-option -gqv @livepicker-client-width' round-trips to the clean nav path and measured failure rate: 0 round-trips = 6/6 pass; 1 round-trip = 5/6 pass (borderline); 2 round-trips = 0/6 pass; 3 round-trips = 0/6 pass. A 20ms sleep (no tmux round-trip) did NOT reproduce it (4/4 pass) -> it is specifically tmux server round-trips advancing the bg job, not latency. The helper adds exactly 2 round-trips -> deterministic break.\n\nATTEMPTED FIXES (all violate or still fail): (1) Combined both reads into one tmux display-message -p round-trip -> still 0/8 pass (1 round-trip is borderline-2-equivalent in server-loop advancement here). (2) Moved the scroll call to AFTER _lp_preview_follow (violates PRP's mandated 'between set_state INDEX and _lp_preview_follow' order) -> still 0/6 pass, because nav #2's own refresh-client -S and run-shell -b also round-trip and the stale fire runs during those too. The race tolerance of this test is effectively ZERO extra tmux round-trips on nav's pre-seq-bump path.\n\nThis is a genuine PLAN conflict, not an implementation error: the PRP's required synchronous scroll read (get_state x2) on the nav path is structurally incompatible with the existing supersession race's tolerance. The file is left in the PRP-verbatim state (all L1 criteria green). Recommended paths forward (require human/orchestrator decision, outside my implementation scope): (a) make the supersession robust to nav latency (e.g., bump STATE_PREVIEW_SEQ EARLIER in nav, before any scroll/state reads, and re-check in _lp_fire_preview) — touches preview.sh/state flow owned by other tasks; (b) compute the nav scroll-into-view WITHOUT tmux round-trips (e.g., have the renderer own STATE_SCROLL advancement on redraw only, dropping the input-handler nav write — but that contradicts PRD §19 §3.32 / this PRP's goal of input-handler as the authoritative writer); or (c) widen test_superseded_preview_noop's tolerance. None of these are within P1.M3.T2.S1's one-file scope.

## Full Agent Output

Now there are 2 failures (the supersession race also destabilizes `test_orig_session_client_aware` due to the same timing sensitivity). The PRP-verbatim implementation fails the suite. This confirms the fundamental plan issue.

Let me produce the final report.

```json
{
   "result": "issue",
   "message": "HALT: fundamental conflict between the PRP's design and an existing race-condition test. The implementation was completed exactly per the PRP verbatim (scripts/input-handler.sh: +source layout.sh; +_lp_scroll_into_view helper using get_state for STATE_CLIENT_WIDTH+STATE_SCROLL; +2 nav call sites; +3 scroll=0 resets). Level 1 passes fully (bash -n clean, shellcheck clean, all 6 grep success-criteria met: source=1, def=1, call-sites=2, resets=3, STATE_SCROLL>=6, tabs-only, 38 additive lines). But Level 2 (tests/run.sh) FAILS reliably: test_superseded_preview_noop (and often test_orig_session_client_aware) fail ~100% of runs with 'stale alpha fire leaked a stray link into the driver'.\n\nROOT CAUSE (proven empirically, not a flake): The PRP's research §7 predicted the suite stays green, but it only reasoned about CORRECTNESS (short lists -> lp_viewport clamps scroll=0 -> no behavior change). It did NOT consider TIMING. The _lp_scroll_into_view helper reads STATE_CLIENT_WIDTH and STATE_SCROLL via get_state, which are each a synchronous tmux show-option round-trip. Every tmux round-trip forces the tmux server to process its event loop, which RUNS pending run-shell -b background jobs. The supersession test fires two rapid next-session navs (seq=1->alpha, seq=2->beta); the stale alpha fire (seq=1) must no-op. It is protected by a mid-function re-check (GUARD 2 in preview.sh) that fires AFTER nav #2 bumps STATE_PREVIEW_SEQ to 2. The window between GUARD 1 (top of preview_main, reads seq) and GUARD 2 (before the unlink) is bridged by nav #2 bumping seq. The helper's 2 extra round-trips on nav #2's PRE-bump path (before _lp_fire_preview bumps seq) both (a) advance alpha's pending fire toward GUARD 2 and (b) delay nav #2's seq bump, so alpha's fire reaches GUARD 2 while seq is still 1 -> it links alpha -> leak.\n\nPROOF OF CAUSATION (clean tree, my change stashed): I added N synthetic 'tmux show-option -gqv @livepicker-client-width' round-trips to the clean nav path and measured failure rate: 0 round-trips = 6/6 pass; 1 round-trip = 5/6 pass (borderline); 2 round-trips = 0/6 pass; 3 round-trips = 0/6 pass. A 20ms sleep (no tmux round-trip) did NOT reproduce it (4/4 pass) -> it is specifically tmux server round-trips advancing the bg job, not latency. The helper adds exactly 2 round-trips -> deterministic break.\n\nATTEMPTED FIXES (all violate or still fail): (1) Combined both reads into one tmux display-message -p round-trip -> still 0/8 pass (1 round-trip is borderline-2-equivalent in server-loop advancement here). (2) Moved the scroll call to AFTER _lp_preview_follow (violates PRP's mandated 'between set_state INDEX and _lp_preview_follow' order) -> still 0/6 pass, because nav #2's own refresh-client -S and run-shell -b also round-trip and the stale fire runs during those too. The race tolerance of this test is effectively ZERO extra tmux round-trips on nav's pre-seq-bump path.\n\nThis is a genuine PLAN conflict, not an implementation error: the PRP's required synchronous scroll read (get_state x2) on the nav path is structurally incompatible with the existing supersession race's tolerance. The file is left in the PRP-verbatim state (all L1 criteria green). Recommended paths forward (require human/orchestrator decision, outside my implementation scope): (a) make the supersession robust to nav latency (e.g., bump STATE_PREVIEW_SEQ EARLIER in nav, before any scroll/state reads, and re-check in _lp_fire_preview) — touches preview.sh/state flow owned by other tasks; (b) compute the nav scroll-into-view WITHOUT tmux round-trips (e.g., have the renderer own STATE_SCROLL advancement on redraw only, dropping the input-handler nav write — but that contradicts PRD §19 §3.32 / this PRP's goal of input-handler as the authoritative writer); or (c) widen test_superseded_preview_noop's tolerance. None of these are within P1.M3.T2.S1's one-file scope."
}
```

**Summary:** I implemented the PRP exactly as specified (Level 1 fully passes — syntax, shellcheck, all 6 grep success criteria). But Level 2 (`tests/run.sh`) fails reliably because the PRP's prescribed `_lp_scroll_into_view` helper adds 2 synchronous tmux round-trips to the nav path, which deterministically breaks the pre-existing `test_superseded_preview_noop` race (proven: 0 extra round-trips = 6/6 pass, 2 extra = 0/6 pass; latency via sleep does NOT reproduce it — only tmux round-trips do, because they advance the pending background fire). The PRP's research §7 prediction of a green suite was based only on correctness reasoning and missed this timing interaction. This is a fundamental plan issue requiring a human/orchestrator decision (make supersession robust to nav latency, drop the nav scroll write, or widen the test) — all three options fall outside this task's one-file scope. The file is left in the faithful PRP-verbatim state.

## Instructions

Review this feedback and create a revised PRP that addresses the issue.
If the issue is fundamentally impossible to resolve, output `"result": "fail"` with an explanation.
